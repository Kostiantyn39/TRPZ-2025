spring.application.name=ftp
# Підключення до бази даних
spring.datasource.url=jdbc:mysql://localhost:3306/ftp_project?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=26032006

# Драйвер (не обов'язково у нових версіях, але корисно знати)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Налаштування Hibernate (JPA)
# update означає, що програма при запуску перевірить таблиці і оновить їх структуру, якщо ви змінили Java-класи
spring.jpa.hibernate.ddl-auto=update

# Відображати SQL запити у консолі (зручно для відлагодження)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true